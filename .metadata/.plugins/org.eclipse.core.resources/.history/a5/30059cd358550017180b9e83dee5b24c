package com.santana.solid.good;

public class LiskovExample {
	public void calculateArea(Shape shape) {
		assert shape.area() == 16 : printError("area", shape);
	}
	private String printError(String indentifier, Shape shape) {
		return "Unexpected value of " + indentifier + "  for instance of " + shape.getClass().getName();
	}
	public static void main(String[] args) {
		LiskovExample liskov = new LiskovExample();
		Rectangle rectangle = new Rectangle();
		rectangle.setHeight(2);
		rectangle.setWidth(8);
		liskov.calculateArea(rectangle);
		Square square = new Square();
		square.setSize(4);
		liskov.calculateArea(new Square());
	}
}
